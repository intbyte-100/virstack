#pragma once

enum set {
    mov,

    ld,
    ldh,
    ldb,

    str,
    strh,
    strb,

    add,
    sub,
    mul,
    div,

    printi,

    cmp,
    cmpl,
    cmpb,
    not,
    jmp,

    exit = 255
};


